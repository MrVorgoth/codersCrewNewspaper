import React from 'react';

export default() =>{
    return(
        <div>
            <p className="line-1 anim-typewriter">
                No article has been found. Try again.
          </p>
        </div>
    );
}